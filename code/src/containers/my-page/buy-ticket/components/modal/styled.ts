import { Modal } from 'antd'
import styled from 'styled-components'

export const ModalStyled = styled(Modal)`
  .ant-modal-content {
    border-radius: 20px;
    overflow: hidden;
  }
  margin-top: 20px;
  margin-bottom: 20px;
`

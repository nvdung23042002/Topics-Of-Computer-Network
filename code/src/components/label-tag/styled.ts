import styled from 'styled-components'

export const ProductStatusStyled = styled.span`
  font-weight: 500;
  font-size: 12px;
  line-height: 16px;
  padding: 0.25rem 0.625rem;
  border-radius: 999px;
  color: #ffff;
`

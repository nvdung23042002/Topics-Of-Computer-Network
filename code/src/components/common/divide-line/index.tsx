import { DividerProps } from 'antd'
import React from 'react'
import { DivideLineStyled } from './styled'

const DivideLine = (props: DividerProps) => {
  return <DivideLineStyled {...props} />
}

export default DivideLine

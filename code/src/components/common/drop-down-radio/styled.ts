import { Card } from 'antd'
import styled from 'styled-components'

export const DropDownContentStyled = styled(Card)`
  box-shadow: 0px 0px 6px 0px #00000040;
  .ant-card-body {
    padding: 25px 20px;
  }
  .radio-item {
    min-width: 250px;
  }
`

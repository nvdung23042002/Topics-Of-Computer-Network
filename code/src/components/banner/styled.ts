import styled from 'styled-components'

export const BannerStyled = styled.div`
  display: block;
  outline: none;
  border-radius: 10px;
  overflow: hidden;
  img {
    pointer-events: none;
    border-radius: 10px;
  }
`
